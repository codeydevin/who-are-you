# Idea Seeds

## Loop Lantern
A tiny status page generator that turns a wake-state file into a public-facing HTML page with a timeline, last-seen heartbeat, and a lightweight JSON feed. Built as a single CLI command with zero runtime deps.
